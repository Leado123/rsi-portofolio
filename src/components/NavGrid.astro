---
---

<div class="nav-grid-container">
  <div class="nav-card fade-in-item" style="animation-delay: 0.1s;">
    <div class="w-full flex items-center justify-between gap-3 mb-2">
      <a 
        class="font-semibold text-lg text-gray-900 hover:text-blue-600 transition-colors" 
        href="https://git.leowen.me" 
        target="_blank"
        rel="noopener noreferrer"
      >
        My Gitea
      </a>
      <img 
        class="h-10 w-10 object-contain rounded-lg bg-white p-1" 
        src="https://upload.wikimedia.org/wikipedia/commons/thumb/b/bb/Gitea_Logo.svg/960px-Gitea_Logo.svg.png?20240428233230" 
        alt="Gitea Logo"
        loading="lazy"
      />
    </div>
    <p class="text-sm text-gray-600 leading-relaxed">
      Where I store my bioinformatic Jupyter notebooks, past projects, etc.
    </p>
  </div>

  <div class="nav-card fade-in-item" style="animation-delay: 0.25s;">
    <div class="w-full flex items-center justify-between gap-3 mb-2">
      <a 
        class="font-semibold text-lg text-gray-900 hover:text-blue-600 transition-colors" 
        href="https://Leado123.github.io" 
        target="_blank"
        rel="noopener noreferrer"
      >
        Sharesyllabus.me
      </a>
      <img 
        class="h-10 w-10 object-contain rounded-lg bg-white p-1" 
        src="https://leado123.github.io/logo_borderless.png" 
        alt="Sharesyllabus Logo"
        loading="lazy"
      />
    </div>
    <p class="text-sm text-gray-600 leading-relaxed">
      An alternative to Rate My Professor that uses syllabi. Used to be hosted at sharesyllabus.me, now archived.
    </p>
  </div>

  <div class="nav-card fade-in-item" style="animation-delay: 0.4s;">
    <div class="w-full flex items-center justify-between gap-3 mb-2">
      <a 
        class="font-semibold text-lg text-gray-900 hover:text-blue-600 transition-colors" 
        href="https://git.leowen.me/liyaowhen/youtubecollector-git" 
        target="_blank"
        rel="noopener noreferrer"
      >
        Youtube Music Downloader
      </a>
      <img 
        class="h-10 w-10 object-contain rounded-lg bg-white p-1" 
        src="https://gitlab.com/uploads/-/system/project/avatar/59940538/logo.png?width=48" 
        alt="Youtube Downloader Logo"
        loading="lazy"
      />
    </div>
    <p class="text-sm text-gray-600 leading-relaxed">
      A GTK based application that downloads music from youtube sources through interaction with yt-dl
    </p>
  </div>
</div>

<style>
  .nav-grid-container {
    display: grid;
    grid-template-columns: repeat(1, minmax(0, 1fr));
    gap: 1rem;
    margin-top: 1rem;
    width: 100%;
  }

  @media (min-width: 768px) {
    .nav-grid-container {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }

  .nav-card {
    cursor: pointer;
    display: flex;
    flex-direction: column;
    width: 100%;
    border: 1px solid rgba(229, 231, 235, 0.6);
    padding: 1rem;
    border-radius: 0.75rem;
    background-color: rgba(255, 255, 255, 0.8);
    backdrop-filter: blur(12px);
    box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
    transition: all 0.2s ease;
  }

  .nav-card:hover {
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    transform: translateY(-2px) scale(1.02);
  }

  .nav-card:active {
    transform: translateY(0) scale(0.98);
  }

  .fade-in-item {
    animation: fadeInUp 0.6s ease-out forwards;
    opacity: 0;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(15px) scale(0.96);
    }
    to {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }
</style>

<script>
  // Add click handlers for cards
  document.addEventListener('DOMContentLoaded', () => {
    const cards = document.querySelectorAll('.nav-card');
    cards.forEach((card, index) => {
      const link = card.querySelector('a');
      if (link) {
        card.addEventListener('click', (e) => {
          // Don't navigate if clicking directly on the link (it will handle its own navigation)
          if (e.target !== link && !link.contains(e.target as Node)) {
            window.open(link.href, '_blank', 'noopener,noreferrer');
          }
        });
      }
    });
  });
</script>
